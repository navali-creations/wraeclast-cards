# Fonts live under assets/fonts/ and are content-hashed, so any filename here
# is safe to cache indefinitely regardless of what the hash currently is.
# JS/CSS bundles stay directly under assets/ and are intentionally excluded so
# users get updates without clearing their cache.
/assets/fonts/*
  Cache-Control: public, max-age=31536000, immutable
